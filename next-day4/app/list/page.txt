import Link from "next/link";

const todos = [
  { id: 1, title: "Learn Next.js" },
  { id: 2, title: "Practice Routing" },
  { id: 3, title: "Build Project" },
];

export default function ListPage() {
  return (
    <>
      <h1>📋 Todo List</h1>

      <ul>
        {todos.map(todo => (
          <li key={todo.id}>
            <Link href={`/detail/${todo.id}`}>
              {todo.title}
            </Link>
          </li>
        ))}
      </ul>
    </>
  );
}
